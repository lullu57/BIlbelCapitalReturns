[pytest]
# Coverage and parallel execution are optional - use: pytest --cov --cov-branch
# Basic options that always work:
addopts = -v --tb=short
filterwarnings = ignore::UserWarning
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*